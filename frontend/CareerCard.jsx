function CareerCard({ title }) {
  return (
    <div style={{
      background: "#0f172a",
      color: "white",
      padding: "10px",
      margin: "10px",
      borderRadius: "8px"
    }}>
      {title}
    </div>
  );
}

export default CareerCard;
